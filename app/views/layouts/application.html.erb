<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Llm Api Call Meta Server" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# FontAwesome 7.0.1 CDN (provided cdnjs) %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css" integrity="sha512-2SwdPD6INVrV/lHTZbO2nodKhrnDdJK9/kg2XD1r9uGqPo1cUbujc+IYdlYdEErWNu69gVcYgdxlmVmzTWnetw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <!-- Navigation Bar -->
    <nav class="bg-blue-600 shadow-lg">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to "LLM API Call Meta Server", root_path, class: "text-white text-xl font-bold hover:text-blue-200" %>
          </div>
          <!-- Desktop Navigation -->
          <div class="hidden md:flex items-center space-x-4">
            <% if user_signed_in? %>
              <!-- User Dropdown -->
              <div class="relative">
                <button popovertarget="menu-popover" class="flex items-center text-white hover:text-blue-200 focus:outline-none">
                  <span class="mr-1"><%= current_user.email %></span>
                  <i class="fa-solid fa-chevron-down h-4 w-4"></i>
                </button>

                <div popover id="menu-popover">
                </div>
              </div>
            <% else %>
              <%= form_with url: user_google_oauth2_omniauth_authorize_path, method: :post, local: true, class: "w-full", data: { turbo: false } do |form| %>
                <%= button_tag type: "submit", class: "text-white hover:text-blue-200 px-3 py-2 rounded-md text-sm font-medium bg-transparent border-0 cursor-pointer" do %>
                  <i class="fa-brands fa-google mr-2"></i>Sign in with Google
                <% end %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= notice %></span>
        <span class="absolute top-0 bottom-0 right-0 px-4 py-3">
          <i class="fa-solid fa-xmark h-6 w-6 text-green-500 cursor-pointer" role="button" onclick="this.parentElement.parentElement.style.display='none'"></i>
        </span>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= alert %></span>
        <span class="absolute top-0 bottom-0 right-0 px-4 py-3">
          <i class="fa-solid fa-xmark h-6 w-6 text-red-500 cursor-pointer" role="button" onclick="this.parentElement.parentElement.style.display='none'"></i>
        </span>
      </div>
    <% end %>
    <main class="max-w-7xl mx-auto mt-8 px-4 sm:px-6 lg:px-8">
      <%= yield %>
    </main>
    <!-- Footer -->
    <footer class="bg-gray-100 mt-12 py-8">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="md:flex md:justify-between">
          <div class="mb-4 md:mb-0">
            <h6 class="text-lg font-semibold text-gray-900">LLM API Call Meta Server</h6>
            <p class="text-sm text-gray-600">A platform for securely managing various LLM API keys</p>
          </div>
          <div class="text-left md:text-right">
            <p class="text-sm text-gray-600">
              &copy; <%= Date.current.year %> DBCLS
            </p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
